<nav class="navbar navbar-expand-lg navbar-dark bg-success">
  <div class="container">
    <a class="navbar-brand" href="/">Foro Estudiantil</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
            aria-expanded="false">
            Sesión
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="/login">Cerrar sesión</a>
            <a class="dropdown-item" href="/editUser/{{user.identificacionUsuario}}">Ver perfil</a>
            <div class="dropdown-divider"></div>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container p-4" style="margin-top: 50px;">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <br />
                <div class="mx-auto">
                    <img src="/images/logoPoli.png" alt="img_cool_work" style="height: 5rem;">
                </div>

                <div class="card-body">
                    <div class="text-center">
                        <h3>¡Bienvenido!</h3>
                        <h4>{{user.nombreUsuario}}</h4>
                        <p>Seleccione la acción que desea hacer</p>
                    </div>

                    <form action="/threads/{{user.identificacionUsuario}}" method="GET">
                        <button class="btn btn-success btn-block">
                            Mirar hilos creados
                        </button>
                    </form>

                    <br/>

                    <form action="/links/add/{{user.identificacionUsuario}}" method="GET">
                        <button class="btn btn-success btn-block">
                            Crear un hilo
                        </button>
                    </form>
                    
                    <br/>

                    <form action="/editUser/{{user.identificacionUsuario}}" method="GET">
                        <button class="btn btn-success btn-block">
                            Mirar perfil
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>